				<div class="media user-media well-small">
					<a class="user-link" href="{{ userInfo.href }}">
						<img class="media-object img-thumbnail user-img" alt="User Picture" src="{{ asset(userInfo.img) }}" />
					</a>
					<br />
					<div class="media-body">
						<h5 class="media-heading">{{ userInfo.name }}</h5>
						<ul class="list-unstyled user-info">
							<li>
								<a class="btn {{ userInfo.class }} btn-xs btn-circle" style="width: 10px;height: 12px;"></a> {{ userInfo.status }}
							</li>
						</ul>
					</div>
					<br />
				</div>