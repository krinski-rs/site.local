			<div id="content">
				<div class="inner" style="min-height:1200px;">
{% if attribute(content, 'title') is defined %}
					<div class="row">
						<div class="col-lg-12">
							<h2>{{ content.title|raw }}</h2>
						</div>
					</div>
					<hr />
{% endif %}
				</div>
			</div>
